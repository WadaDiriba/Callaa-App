class TaskEntity {
  final String id;
  final String title;
  final double rewardAmount;
  final DateTime taskDate;

  TaskEntity({

    required this.id, 
    required this.title,
     required this.rewardAmount,
     required this.taskDate
     }
     );
}